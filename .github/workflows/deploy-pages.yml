name: Deprecated v43 mirror publisher
on:
  workflow_dispatch:

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Report superseded workflow
        run: echo "Superseded by the Step 9 R4 direct portable deployment."
